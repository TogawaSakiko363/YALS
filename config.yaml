# YALS Server Configuration

# Server settings
server:
  host: "0.0.0.0"
  port: 443
  password: "your_password"
  log_level: "info"  # debug, info, warn, error
  tls: true # Set to true to enable HTTPS/TLS
  tls_cert_file: "./cert.pem"  # Path to TLS certificate file
  tls_key_file: "./key.pem"    # Path to TLS private key file

# WebSocket settings
websocket:
  ping_interval: 30  # seconds
  pong_wait: 60      # seconds

# Connection settings
connection:
  keepalive: 86400  # seconds (24 hours), set to 0 to never delete

